# Examples

This folder contains a small visual subset of exported tiles used for documentation and README figures. The examples are not intended to train or benchmark a model.

Regenerate the example tile mosaic with:

```bash
python scripts/make_public_figures.py
```
